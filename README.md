# Orbit Aluminum Industries static build

Open `index.html` with a local server, e.g. `python3 -m http.server 4173`.

Content is structured in `content/pages.json` and translation placeholders in `content/i18n/*.json`.
